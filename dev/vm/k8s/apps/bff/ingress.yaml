apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bff
  namespace: apps
spec:
  rules:
    - host: bff.fin.soul.test
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bff
                port:
                  number: 80
