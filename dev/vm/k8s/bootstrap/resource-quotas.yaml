apiVersion: v1
kind: ResourceQuota
metadata:
  name: default-quota
  namespace: apps
spec:
  hard:
    requests.cpu: "6"
    requests.memory: 12Gi
    limits.cpu: "8"
    limits.memory: 14Gi
    pods: "60"
